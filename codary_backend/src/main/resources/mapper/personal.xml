<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.spring.web.dao.PersonalDao">
	
	<select id="mywork" resultType="BlogContentsDto"> 
		select *
		from blogcontents 
		where blog_id = #{blogId}
	</select>
	
	<!-- personal blog => me -->
	<!-- <select id="mywork" resultType="BlogContentsDto"> 
		select *
		from blogcontents 
		where blog_id = (
			select blog_id 
			from blog 
			where blog_id=#{blogId} 
		)
	</select>-->
	 
	
	<!-- personal blog => other -->
	<!-- 
	<select id="otherwork" resultType="BlogContentsDto"> 
		select *
		from blogcontents 
		where blog_id = (
			select blog_id 
			from user 
			where uid=(
				select uid 
				from userinfo 
				where nickname=#{nickname}
				)
		)
	</select>
	 -->
</mapper>