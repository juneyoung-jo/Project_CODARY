<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.spring.web.dao.PersonalDao">
	
	<select id="showBlogContents" resultType="BlogContentsDto"> 
		select *
		from blogcontents 
		where blog_id = #{blogId}
	</select>
	
	<select id="showMemo" resultType="MemoContentsDto">
		select * 
		from memocontents
		where memo_id = #{memoId}
	</select>
	
	<select id="likeBloger" resultType="BlogDto">
		select * 
		from blog inner join blogerlike using (blog_id)
		where blogerlike.uid = #{uid};
	</select>
	
	<select id="likeBlogContents" resultType="BlogContentsDto">
		select * 
		from blogcontents inner join blogcontentslike using (blog_contents_id)
		where blogcontentslike.uid = #{uid};
	</select>
</mapper>