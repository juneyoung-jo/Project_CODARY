<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.spring.web.dao.PersonalDao">
	
	<select id="showBlogContents" resultType="BlogContentsDto"> 
		select *
		from blogcontents 
		where blog_id = #{blogId}
	</select>
	
	<select id="showMemo" resultType="MemoDto">
		select * 
		from memo
		where memo_id = #{memoId}
	</select>
	
	<select id="likeBloger" resultType="BlogerLikeDto">
		select *
		from blogerlike
		where uid = #{uid}
	</select>
</mapper>